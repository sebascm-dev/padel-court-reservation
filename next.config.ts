/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['nwozeonjgekmhkthkonu.supabase.co'], // Dominio de tu proyecto Supabase
    },
}

module.exports = nextConfig;
